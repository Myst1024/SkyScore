# SkyScore Agent Instructions

## Workflow Rules

### Code Validation

After making any code changes (creating, editing, or updating files), always run the following command to validate the code:

```bash
bun check
```

This ensures that all code changes pass linting and type checking before completing the task.
